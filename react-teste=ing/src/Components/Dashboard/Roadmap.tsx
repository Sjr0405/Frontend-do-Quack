import React from 'react';

const Roadmap = ({ changeSection }: { changeSection: (section: string) => void }) => {
  return (
    <div>
      <h1>Olá Mundo - Roadmap</h1>
    </div>
  );
};

export default Roadmap;
